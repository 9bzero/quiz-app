# quiz-app

Multiple-choice quiz with a countdown timer.

## Features

- Create custom quizzes (question, 4 options, correct answer)
- Built-in sample quizzes (general knowledge, TypeScript, history)
- 30-second timer per question
- Score summary with correct/incorrect breakdown
- Review mode: see which questions you missed

## Run

```bash
npm install && npm run dev
```
